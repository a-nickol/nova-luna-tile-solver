import { TileBgColorPipe } from "./tile-bg-color.pipe";

describe("TileBgColorPipe", () => {
  it("create an instance", () => {
    const pipe = new TileBgColorPipe();
    expect(pipe).toBeTruthy();
  });
});
